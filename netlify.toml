[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/landing-page run build"
  publish = "artifacts/landing-page/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
