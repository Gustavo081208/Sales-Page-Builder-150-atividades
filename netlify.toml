[build]
  base = "artifacts/landing-page"
  command = "cd ../.. && pnpm install && pnpm --filter @workspace/landing-page run build"
  publish = "artifacts/landing-page/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"
